# 如何编译 Server Manager

## ⚠️ 重要：跨平台编译限制

### 编译规则（非常重要！）

| 当前系统 | 可以编译的目标 | 不能编译的目标 |
|---------|--------------|--------------|
| **Linux** | ✅ Linux AppImage<br>✅ Linux .deb | ❌ Windows .exe<br>❌ macOS .app |
| **Windows** | ✅ Windows .exe | ❌ macOS .app<br>⚠️ Linux（需要配置） |
| **macOS** | ✅ macOS .app<br>✅ Windows .exe<br>✅ Linux | 全部可以 |

**为什么不能跨平台编译？**
- macOS 的 DMG 打包需要 macOS 系统特有的工具（dmg-license、Xcode）
- Windows 在 Linux 上编译需要 wine（配置复杂，容易出错）
- **最佳实践：在目标平台上编译目标平台的应用**

---

## 🚀 编译方法

### 方法 1：使用专用编译脚本（推荐）

我已经为每个平台创建了专用脚本：

#### 在 Linux 上编译（当前环境）
```bash
chmod +x build-linux.sh
./build-linux.sh
```
**输出**：
- ✅ `release/Server Manager-1.0.0.AppImage` (111 MB)
- ✅ `release/server-manager_1.0.0_amd64.deb`

---

#### 在 Windows 上编译
```cmd
build-windows.bat
```
**输出**：
- ✅ `release/Server Manager Setup 1.0.0.exe` (安装版)
- ✅ `release/Server Manager 1.0.0.exe` (便携版)

---

#### 在 macOS 上编译
```bash
chmod +x build-macos.sh
./build-macos.sh
```
**输出**：
- ✅ `release/Server Manager-1.0.0.dmg`
- ✅ `release/Server Manager-1.0.0-mac.zip`

---

### 方法 2：手动编译命令

#### 编译 Linux（仅在 Linux 系统）
```bash
npm install
npm run build
npm run electron:build
```

#### 编译 Windows（仅在 Windows 系统）
```bash
npm install
npm run build
npm run electron:build:win
```

#### 编译 macOS（仅在 macOS 系统）
```bash
npm install
npm run build
npm run electron:build:mac
```

---

## 🎯 当前环境：Linux

### ✅ 您现在可以做的：

**1. 编译 Linux 版本**
```bash
chmod +x build-linux.sh
./build-linux.sh
```

**2. 查看已编译的文件**
```bash
ls -lh release/
```

**3. 运行 Linux 版本**
```bash
chmod +x "release/Server Manager-1.0.0.AppImage"
./"release/Server Manager-1.0.0.AppImage"
```

### ❌ 您现在不能做的：

- ❌ 编译 Windows .exe（会报 `wine is required` 错误）
- ❌ 编译 macOS .app（会报 `Cannot find module 'dmg-license'` 错误）

---

## 🌐 如何编译所有平台？

### 方案 1：使用 GitHub Actions（强烈推荐）

**优点**：
- ✅ 自动编译所有平台（Linux + Windows + macOS）
- ✅ 完全免费
- ✅ 不需要自己有多个操作系统

**步骤**：

1. **创建 GitHub Actions 配置文件**

创建文件 `.github/workflows/build.yml`：

```yaml
name: Build All Platforms

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          
      - name: Install dependencies
        run: npm install
        
      - name: Build application
        run: npm run electron:build
        
      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.os }}
          path: release/*
```

2. **推送代码并打标签**

```bash
# 提交代码
git add .
git commit -m "Add build workflow"
git push

# 打版本标签
git tag v1.0.0
git push origin v1.0.0
```

3. **等待编译完成**

- 访问 GitHub 仓库的 Actions 页面
- 等待约 10-15 分钟
- 下载所有平台的编译文件

---

### 方案 2：在不同系统上分别编译

如果您有权限访问多个系统：

#### 步骤 1：准备代码
```bash
# 推送到 Git 仓库
git add .
git commit -m "Ready for build"
git push
```

#### 步骤 2：在 Windows 电脑上
```bash
git clone <your-repo-url>
cd server-manager
npm install
npm run electron:build:win
# 获取 release/ 目录下的 .exe 文件
```

#### 步骤 3：在 macOS 电脑上
```bash
git clone <your-repo-url>
cd server-manager
npm install
npm run electron:build:mac
# 获取 release/ 目录下的 .dmg 文件
```

#### 步骤 4：在 Linux 电脑上（已完成）
```bash
# 您已经有了 Linux 版本
# release/Server Manager-1.0.0.AppImage
```

---

## 📦 编译输出文件说明

### Linux 版本
- **AppImage**: 通用格式，所有 Linux 发行版都支持
- **.deb**: Ubuntu/Debian 专用安装包

### Windows 版本
- **Setup.exe**: 安装版，推荐分发给普通用户
- **.exe**: 便携版，无需安装，适合U盘携带

### macOS 版本
- **.dmg**: DMG 安装包，推荐分发
- **.zip**: ZIP 压缩包，解压后可直接使用

---

## 🧪 测试编译结果

### 测试 Linux 版本
```bash
chmod +x "release/Server Manager-1.0.0.AppImage"
./"release/Server Manager-1.0.0.AppImage"
```

### 测试 Windows 版本
```cmd
# 双击运行
release\Server Manager 1.0.0.exe
```

### 测试 macOS 版本
```bash
# 双击打开 DMG，拖到 Applications 后
open "/Applications/Server Manager.app"
```

---

## 📊 编译时间参考

| 平台 | 首次编译 | 后续编译 |
|-----|---------|---------|
| Linux | 8-10 分钟 | 3-5 分钟 |
| Windows | 10-12 分钟 | 4-6 分钟 |
| macOS | 10-15 分钟 | 5-7 分钟 |

---

## ❓ 常见问题

### Q1: 为什么会出现 "Cannot find module 'dmg-license'" 错误？

**答**：因为您在 Linux 系统上尝试编译 macOS 应用。

**解决方案**：
- 选项 1：只编译 Linux 版本（使用 `build-linux.sh`）
- 选项 2：使用 GitHub Actions 自动编译 macOS
- 选项 3：在 Mac 电脑上编译

---

### Q2: 为什么会出现 "wine is required" 错误？

**答**：因为在 Linux 上编译 Windows 需要 wine 模拟器。

**解决方案**：
- 选项 1：只编译 Linux 版本
- 选项 2：使用 GitHub Actions
- 选项 3：在 Windows 电脑上编译

---

### Q3: 我只有 Linux 系统，怎么编译所有平台？

**答**：使用 GitHub Actions！完全免费，自动编译所有平台。

---

### Q4: GitHub Actions 是免费的吗？

**答**：是的！GitHub 提供：
- 公开仓库：无限制免费
- 私有仓库：每月 2000 分钟免费

---

## 🎯 推荐流程

### 如果您只需要 Linux 版本（当前可用）：
```bash
./build-linux.sh
```
✅ 3-5 分钟后得到 AppImage

### 如果您需要所有平台（推荐）：
1. 配置 GitHub Actions（5 分钟）
2. 推送代码并打标签（1 分钟）
3. 等待自动编译（15 分钟）
4. 下载所有平台的编译文件

**总时间**: 约 20 分钟，全自动！

---

## 📞 需要帮助？

如果遇到问题，请查看：
- `CROSS-PLATFORM-BUILD.md` - 跨平台编译详细说明
- `COMPILE-GUIDE.md` - 完整编译指南
- `BUILD-AND-PACKAGE-GUIDE.md` - 打包和执行指南

---

## ✅ 当前状态

- ✅ Linux AppImage 已编译完成（111 MB）
- ⏳ Windows 和 macOS 需要在对应平台编译
- 🚀 推荐使用 GitHub Actions 自动编译所有平台

**文件位置**: `release/Server Manager-1.0.0.AppImage`

**立即运行**:
```bash
chmod +x "release/Server Manager-1.0.0.AppImage"
./"release/Server Manager-1.0.0.AppImage"
```
