# 完整编译流程指南 - 从 ClackyAI 到获得所有平台安装包

## 🎯 目标

从当前 ClackyAI 环境出发，获得：
- ✅ Windows .exe 安装包
- ✅ macOS .dmg 安装包  
- ✅ Linux AppImage 安装包

## ⚠️ 重要说明

**为什么不能直接在 ClackyAI 编译 macOS 和 Windows？**

- ClackyAI 是 **Linux 云环境**（路径：`/home/runner/app`）
- macOS 的 DMG 打包工具 `dmg-license` **只能在真实的 macOS 系统上运行**
- Windows 编译需要 wine 模拟器，配置复杂且不稳定

**最佳解决方案**：使用 GitHub Actions，让 GitHub 的服务器自动在真实的 Windows、macOS、Linux 系统上分别编译！

---

## 📋 完整步骤

### 第一步：创建 GitHub 仓库

#### 1.1 在 GitHub 上创建新仓库

1. 访问 https://github.com/
2. 点击右上角 **+** → **New repository**
3. 填写信息：
   - **Repository name**: `server-manager`（或您喜欢的名字）
   - **Description**: `A desktop application for server management`
   - **Public** 或 **Private**（推荐 Public，可免费无限使用 Actions）
4. **不要**勾选 "Initialize this repository with..."
5. 点击 **Create repository**

#### 1.2 记录仓库地址

创建后会显示仓库地址，例如：
```
https://github.com/your-username/server-manager.git
```

记下这个地址，下一步要用。

---

### 第二步：在 ClackyAI 中推送代码

在 ClackyAI 终端执行以下命令：

```bash
# 1. 配置 Git 用户信息（如果还没配置）
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 2. 初始化仓库（如果还没初始化）
git init

# 3. 添加远程仓库（替换成您的仓库地址）
git remote add origin https://github.com/your-username/server-manager.git

# 4. 添加所有文件
git add .

# 5. 提交
git commit -m "Initial commit with SQLite database and build workflow"

# 6. 推送到 GitHub
git push -u origin main
```

**如果推送时要求输入密码**：
- GitHub 已不支持密码认证
- 需要使用 Personal Access Token
- 或者在 ClackyAI 界面右下角点击 "Commit changes" 按钮

---

### 第三步：验证 GitHub Actions 配置

#### 3.1 确认文件已上传

访问您的 GitHub 仓库，确认以下文件存在：
- ✅ `.github/workflows/build.yml`
- ✅ `package.json`
- ✅ 所有源代码文件

#### 3.2 查看 Actions 页面

1. 在 GitHub 仓库页面，点击顶部的 **Actions** 标签
2. 应该能看到 **Build All Platforms** 工作流

---

### 第四步：触发编译

有两种方式触发编译：

#### 方式 1：手动触发（推荐）

1. 访问 GitHub 仓库 → **Actions** 标签
2. 左侧点击 **Build All Platforms**
3. 右侧点击 **Run workflow** 下拉按钮
4. 点击绿色的 **Run workflow** 按钮

✅ 开始编译！

#### 方式 2：通过 Git 标签触发

在 ClackyAI 终端或使用 Thread 右下角的 "Commit changes" 按钮后：

```bash
# 打版本标签
git tag v1.0.0
git push origin v1.0.0
```

✅ 自动触发编译！

---

### 第五步：等待编译完成

#### 5.1 查看编译进度

1. 访问 **Actions** 页面
2. 点击最新的 workflow run（工作流运行）
3. 可以看到三个并行的编译任务：
   - 🐧 **ubuntu-latest** (Linux)
   - 🪟 **windows-latest** (Windows)  
   - 🍎 **macos-latest** (macOS)

#### 5.2 编译时间

- Linux: 5-8 分钟
- Windows: 6-10 分钟
- macOS: 8-12 分钟

**总时间**: 约 **15-20 分钟**（并行进行）

#### 5.3 查看实时日志

点击任意一个任务可以查看详细的编译日志。

---

### 第六步：下载编译结果

#### 6.1 等待全部完成

确保三个任务都显示 ✅ 绿色对勾。

#### 6.2 下载 Artifacts

滚动到页面底部，找到 **Artifacts** 区域：

- 📦 **linux-build** - 点击下载 Linux 版本
- 📦 **windows-build** - 点击下载 Windows 版本
- 📦 **macos-build** - 点击下载 macOS 版本

#### 6.3 解压文件

下载后会得到三个 ZIP 文件：

```bash
unzip linux-build.zip
# 得到：
# - Server Manager-1.0.0.AppImage
# - server-manager_1.0.0_amd64.deb

unzip windows-build.zip
# 得到：
# - Server Manager Setup 1.0.0.exe
# - Server Manager 1.0.0.exe

unzip macos-build.zip
# 得到：
# - Server Manager-1.0.0.dmg
# - Server Manager-1.0.0-mac.zip
```

✅ **完成！现在您有了所有平台的安装包！**

---

## 📦 文件说明

### Linux 版本
- **Server Manager-1.0.0.AppImage** (111 MB)
  - 通用格式，适用所有 Linux 发行版
  - 使用：`chmod +x *.AppImage && ./Server\ Manager-1.0.0.AppImage`

- **server-manager_1.0.0_amd64.deb** (100 MB)
  - Ubuntu/Debian 安装包
  - 使用：`sudo dpkg -i server-manager_1.0.0_amd64.deb`

### Windows 版本
- **Server Manager Setup 1.0.0.exe** (80 MB)
  - 安装版（推荐分发）
  - 双击运行安装向导

- **Server Manager 1.0.0.exe** (150 MB)
  - 便携版
  - 无需安装，双击直接运行

### macOS 版本
- **Server Manager-1.0.0.dmg** (90 MB)
  - DMG 安装包（推荐分发）
  - 双击打开，拖到 Applications 文件夹

- **Server Manager-1.0.0-mac.zip** (100 MB)
  - ZIP 压缩包
  - 解压后可直接使用

---

## 🔄 后续更新流程

当您需要发布新版本时：

```bash
# 1. 在 ClackyAI 中开发和测试
npm run dev

# 2. 修改版本号
# 编辑 package.json，修改 "version": "1.0.1"

# 3. 提交更改
git add .
git commit -m "Release v1.0.1"
git push

# 4. 打新标签
git tag v1.0.1
git push origin v1.0.1

# 5. 等待 GitHub Actions 自动编译

# 6. 下载新版本的安装包
```

---

## ❓ 常见问题

### Q1: 推送代码时要求输入用户名密码？

**方法 1**：使用 ClackyAI 的 Commit 按钮
- 在 Thread 页面右下角点击 "Commit changes"

**方法 2**：使用 Personal Access Token
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. 勾选 `repo` 权限
4. 使用 token 作为密码

### Q2: GitHub Actions 编译失败？

1. 查看 Actions 页面的错误日志
2. 常见问题：
   - 依赖安装失败 → 检查网络或依赖版本
   - 构建错误 → 检查代码语法
   - 权限问题 → 检查仓库设置 → Actions → General → Workflow permissions

### Q3: Actions 显示 "Workflow not found"？

确保 `.github/workflows/build.yml` 文件已正确推送到 GitHub。

### Q4: 可以编译其他版本吗？

可以！修改 `package.json` 中的版本号，然后重新打标签触发编译。

### Q5: 编译太慢？

GitHub 免费 runner 的速度已经很不错了。如果需要更快：
- 使用缓存（我已在配置中包含）
- 升级到 GitHub Pro 使用更快的 runner

### Q6: 能自动发布到 Release 吗？

可以！修改 `.github/workflows/build.yml`，添加：

```yaml
- name: Create Release
  uses: softprops/action-gh-release@v1
  if: startsWith(github.ref, 'refs/tags/')
  with:
    files: release/*
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

---

## 💰 费用说明

### GitHub Actions 免费额度

| 仓库类型 | 免费额度 |
|---------|---------|
| **公开仓库** | ✅ 无限制免费 |
| **私有仓库** | 每月 2000 分钟 |

**本项目编译一次**：约 15-20 分钟（三个平台并行）
**私有仓库每月可编译**：约 100 次

完全够用！

---

## ✅ 检查清单

使用此检查清单确保每个步骤都完成：

- [ ] 在 GitHub 创建了仓库
- [ ] 在 ClackyAI 配置了 Git remote
- [ ] 推送了所有代码到 GitHub
- [ ] 确认 `.github/workflows/build.yml` 文件存在
- [ ] 在 Actions 页面触发了编译
- [ ] 等待编译完成（三个绿色对勾）
- [ ] 下载了所有平台的 Artifacts
- [ ] 解压并测试了安装包

---

## 🎉 完成！

现在您有了：
- ✅ Windows 安装包（.exe）
- ✅ macOS 安装包（.dmg）
- ✅ Linux 安装包（.AppImage 和 .deb）

可以分发给用户了！

---

## 📞 需要帮助？

如果在任何步骤遇到问题，请：

1. 查看 GitHub Actions 的错误日志
2. 确认所有文件都已推送
3. 检查网络连接
4. 查看 GitHub Actions 文档：https://docs.github.com/actions

---

**祝您编译顺利！🚀**
